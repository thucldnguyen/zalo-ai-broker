---
phase: 01-verify-ship-llm-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
autonomous: true
requirements:
  - LLM-01
  - LLM-02
  - LLM-03
  - TEST-01
  - TEST-02
  - TEST-03
  - ENV-01
  - ENV-02

must_haves:
  truths:
    - "ListenerAgent LLM extraction path works correctly with mocked provider"
    - "CloserAgent LLM suggestion path works correctly with mocked provider"
    - "Both agents fall back to heuristics when LLM provider raises an exception"
    - "All 13 tests pass with pytest"
    - ".env.example documents ANTHROPIC_API_KEY and model override vars"
    - "Startup log uses single-space format matching ROADMAP success criterion"
  artifacts:
    - path: "main.py"
      provides: "LLM provider startup with correct log format"
      contains: "extraction=%s reply=%s"
    - path: ".env.example"
      provides: "LLM env var documentation"
      contains: "ANTHROPIC_API_KEY"
    - path: "tests/test_basic.py"
      provides: "Mocked LLM path tests for both agents"
      contains: "TestListenerLLMPath"
  key_links:
    - from: "main.py"
      to: "core/llm/provider.py"
      via: "_build_llm_provider imports AnthropicLLMProvider"
      pattern: "from core\\.llm\\.provider import"
    - from: "agents/listener.py"
      to: "core/llm/provider.py"
      via: "llm_provider.extract_message call"
      pattern: "self\\._llm_provider\\.extract_message"
    - from: "agents/closer.py"
      to: "core/llm/provider.py"
      via: "llm_provider.generate_suggestions call"
      pattern: "self\\._llm_provider\\.generate_suggestions"
---

<objective>
Fix the double-space log format in main.py and verify all LLM integration code is correct by running the full test suite.

Purpose: Ensure the codebase matches the ROADMAP success criteria before committing, and confirm all LLM/test/env requirements are satisfied.
Output: main.py with corrected log format; verified passing test suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-verify-ship-llm-integration/01-RESEARCH.md
@main.py
@tests/test_basic.py
@core/llm/provider.py
@agents/listener.py
@agents/closer.py
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix double-space in startup log format string</name>
  <files>main.py</files>
  <read_first>main.py</read_first>
  <action>
In `main.py`, line 51, change the log format string from double-space to single-space:

Before: `"LLM provider: extraction=%s  reply=%s"`
After:  `"LLM provider: extraction=%s reply=%s"`

This is the only code change needed in the entire phase. The ROADMAP success criterion specifies: "App starts and logs 'LLM provider: extraction=claude-haiku-4-5-20251001 reply=claude-haiku-4-5-20251001'". The double-space would cause exact-match verification to fail.
  </action>
  <verify>
    <automated>grep -n "extraction=%s reply=%s" /Users/thucnguyen/Documents/GitHub/zalo-ai-broker/main.py | grep -v "  reply" && echo "PASS: single space" || echo "FAIL: still double space"</automated>
  </verify>
  <acceptance_criteria>
- `grep "extraction=%s  reply=%s" main.py` returns NO matches (double-space gone)
- `grep "extraction=%s reply=%s" main.py` returns exactly 1 match (single-space present)
  </acceptance_criteria>
  <done>The log format string in _build_llm_provider() uses single space between extraction and reply model names</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify all LLM integration requirements</name>
  <files>tests/test_basic.py, core/llm/provider.py, core/llm/schemas.py, core/llm/tools.py, agents/listener.py, agents/closer.py, .env.example</files>
  <read_first>tests/test_basic.py, .env.example</read_first>
  <action>
Run `pytest tests/ -v` and verify all 13 tests pass. This confirms:

- LLM-01: TestListenerLLMPath::test_llm_extraction_used passes (ListenerAgent uses mocked LLM provider)
- LLM-02: TestCloserLLMPath::test_llm_suggestions_used passes (CloserAgent uses mocked LLM provider)
- LLM-03: test_llm_failure_falls_back_to_heuristics and test_llm_failure_falls_back_to_templates pass (fallback works)
- TEST-01: ListenerAgent LLM path has mocked tests (test_llm_extraction_used, test_llm_failure_falls_back_to_heuristics)
- TEST-02: CloserAgent LLM path has mocked tests (test_llm_suggestions_used, test_llm_failure_falls_back_to_templates)
- TEST-03: All 13 tests pass (no regressions)

Also verify ENV requirements by reading files:
- ENV-01: Confirm .env.example contains ANTHROPIC_API_KEY, ANTHROPIC_MODEL_EXTRACTION, ANTHROPIC_MODEL_REPLY
- ENV-02: Confirm main.py logs "heuristic-only mode" when key absent and "LLM provider: extraction=... reply=..." when present

Do NOT modify any files in this task. This is verification only.
  </action>
  <verify>
    <automated>cd /Users/thucnguyen/Documents/GitHub/zalo-ai-broker && python -m pytest tests/ -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
- `pytest tests/ -v` output shows "13 passed" and 0 failures
- `grep "ANTHROPIC_API_KEY" .env.example` returns at least 1 match
- `grep "ANTHROPIC_MODEL_EXTRACTION" .env.example` returns at least 1 match
- `grep "ANTHROPIC_MODEL_REPLY" .env.example` returns at least 1 match
- `grep "heuristic-only mode" main.py` returns at least 1 match
- `grep "LLM provider:" main.py` returns at least 1 match
  </acceptance_criteria>
  <done>All 13 tests pass, .env.example documents all 3 Anthropic vars, main.py logs startup mode correctly</done>
</task>

</tasks>

<verification>
- All 13 tests pass with `pytest tests/ -v`
- main.py log format string has single space (matches ROADMAP criterion)
- .env.example contains ANTHROPIC_API_KEY, ANTHROPIC_MODEL_EXTRACTION, ANTHROPIC_MODEL_REPLY
- main.py contains both "heuristic-only mode" and "LLM provider:" log lines
</verification>

<success_criteria>
- `pytest tests/ -v` exits with code 0 and shows 13 passed
- `grep -c "extraction=%s reply=%s" main.py` returns 1 (single space, not double)
- ENV-01 and ENV-02 confirmed by file inspection
</success_criteria>

<output>
After completion, create `.planning/phases/01-verify-ship-llm-integration/01-01-SUMMARY.md`
</output>
