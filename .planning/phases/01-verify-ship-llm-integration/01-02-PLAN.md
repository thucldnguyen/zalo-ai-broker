---
phase: 01-verify-ship-llm-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - core/llm/__init__.py
  - core/llm/provider.py
  - core/llm/schemas.py
  - core/llm/tools.py
  - pyproject.toml
  - agents/closer.py
  - agents/listener.py
  - main.py
  - requirements.txt
  - tests/test_basic.py
  - .env.example
autonomous: true
requirements:
  - LLM-04

must_haves:
  truths:
    - "core/llm/ directory is tracked by git (all 4 files committed)"
    - "All modified LLM-related files are committed in a single coherent commit"
    - "Unrelated files (core/memory.py, integrations/*, .cursor/, CLAUDE.md) are NOT in the commit"
    - "Tests still pass after commit"
  artifacts:
    - path: "core/llm/__init__.py"
      provides: "Python package marker for core.llm"
    - path: "core/llm/provider.py"
      provides: "LLMProvider Protocol and AnthropicLLMProvider"
      exports: ["LLMProvider", "AnthropicLLMProvider", "DEFAULT_MODEL"]
    - path: "core/llm/schemas.py"
      provides: "Pydantic boundary types for LLM I/O"
      exports: ["MessageExtraction", "SuggestionOutput"]
    - path: "core/llm/tools.py"
      provides: "Anthropic tool schemas and message builders"
      exports: ["EXTRACTION_TOOL", "REPLY_TOOL", "EXTRACTION_SYSTEM_PROMPT", "REPLY_SYSTEM_PROMPT"]
    - path: "pyproject.toml"
      provides: "pytest asyncio_mode=auto config"
      contains: "asyncio_mode"
  key_links:
    - from: "git index"
      to: "core/llm/"
      via: "git add of all 4 files"
      pattern: "core/llm/"
---

<objective>
Stage and commit all LLM integration files as a single coherent git commit, ensuring only LLM-related changes are included.

Purpose: Get core/llm/ and all related modifications tracked in git (LLM-04), keeping the commit focused on the LLM integration without unrelated changes.
Output: A clean git commit containing exactly the 11 LLM-related files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-verify-ship-llm-integration/01-RESEARCH.md
@.planning/phases/01-verify-ship-llm-integration/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage LLM integration files and commit</name>
  <files>
core/llm/__init__.py, core/llm/provider.py, core/llm/schemas.py, core/llm/tools.py,
pyproject.toml, agents/closer.py, agents/listener.py, main.py,
requirements.txt, tests/test_basic.py, .env.example
  </files>
  <read_first>main.py</read_first>
  <action>
Stage exactly these 11 files and commit them. The files fall into three groups:

1. **New untracked files (git add required):**
   - `core/llm/__init__.py` (empty package marker)
   - `core/llm/provider.py` (LLMProvider Protocol + AnthropicLLMProvider)
   - `core/llm/schemas.py` (MessageExtraction, SuggestionOutput Pydantic models)
   - `core/llm/tools.py` (Anthropic tool schemas, system prompts, message builders)
   - `pyproject.toml` (pytest asyncio_mode = "auto")

2. **Modified tracked files:**
   - `agents/closer.py` (LLM provider integration)
   - `agents/listener.py` (LLM provider integration)
   - `main.py` (LLM provider startup, dependency injection)
   - `requirements.txt` (anthropic, pytest-asyncio deps)
   - `tests/test_basic.py` (mocked LLM path tests)
   - `.env.example` (ANTHROPIC_API_KEY and model override vars)

3. **Files to NOT stage (unrelated changes):**
   - `core/memory.py` — unrelated modifications
   - `integrations/zalo_client.py` — unrelated modifications
   - `integrations/zalo_routes.py` — unrelated modifications
   - `.cursor/` — editor config, not project code
   - `CLAUDE.md` — project docs, separate concern

Commands to run:
```bash
git add core/llm/__init__.py core/llm/provider.py core/llm/schemas.py core/llm/tools.py
git add pyproject.toml
git add agents/closer.py agents/listener.py main.py requirements.txt tests/test_basic.py .env.example
```

Then commit with message:
```
feat: add Claude Haiku LLM integration with heuristic fallback

- Add core/llm/ module: AnthropicLLMProvider with retry logic,
  Pydantic boundary schemas, Anthropic tool-use definitions
- Integrate LLM into ListenerAgent (extraction) and CloserAgent
  (reply suggestions) with transparent heuristic fallback on error
- Add mocked LLM path tests for both agents (4 new test cases)
- Configure pytest asyncio_mode=auto in pyproject.toml
- Document ANTHROPIC_API_KEY and model override vars in .env.example
```

After committing, run `pytest tests/ -v` to confirm tests still pass post-commit, and run `git status` to confirm only the unrelated files remain as unstaged changes.
  </action>
  <verify>
    <automated>cd /Users/thucnguyen/Documents/GitHub/zalo-ai-broker && git log --oneline -1 && git show --stat HEAD | head -20 && python -m pytest tests/ -v 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `git log --oneline -1` shows a commit with message starting with "feat: add Claude Haiku LLM integration"
- `git show --stat HEAD` lists exactly 11 files: core/llm/__init__.py, core/llm/provider.py, core/llm/schemas.py, core/llm/tools.py, .env.example, agents/closer.py, agents/listener.py, main.py, pyproject.toml, requirements.txt, tests/test_basic.py
- `git show --stat HEAD` does NOT list core/memory.py, integrations/zalo_client.py, integrations/zalo_routes.py, .cursor/, or CLAUDE.md
- `git ls-files core/llm/` lists all 4 files (confirming they are tracked)
- `pytest tests/ -v` shows 13 passed post-commit
  </acceptance_criteria>
  <done>All LLM integration files committed in a single clean commit; core/llm/ is git-tracked; tests pass; unrelated files remain unstaged</done>
</task>

</tasks>

<verification>
- `git ls-files core/llm/` returns 4 files: __init__.py, provider.py, schemas.py, tools.py
- `git log --oneline -1` shows the LLM integration commit
- `git diff --name-only` shows only unrelated files (core/memory.py, integrations/*)
- `pytest tests/ -v` passes with 13 tests
</verification>

<success_criteria>
- LLM-04 satisfied: core/llm/ is committed and tracked in git
- The commit contains exactly 11 files (no more, no less)
- Post-commit test suite passes (13/13)
- Working tree has no LLM-related uncommitted changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-verify-ship-llm-integration/01-02-SUMMARY.md`
</output>
